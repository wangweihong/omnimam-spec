provider:
  code: modelscope
  name: ModelScope
  category:
    - model-hub
    - inference-platform
engine_types:
  - modelscope_api
  - modelscope_local
adapter:
  code: modelscope_inference
authentication:
  supported_methods:
    - access_token
capability:
  discovery:
    mode: manual
  execution:
    mode: sync_or_async
