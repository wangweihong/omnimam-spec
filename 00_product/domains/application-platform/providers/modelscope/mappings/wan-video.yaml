model:
  provider_model_id: Wan-AI/Wan2.x

request:
  prompt:
    target: input.prompt

  image:
    target: input.image

  num_frames:
    target: parameters.num_frames

  fps:
    target: parameters.fps

response:
  video:
    source: output.video_url
