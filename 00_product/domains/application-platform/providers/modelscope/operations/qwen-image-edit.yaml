operation:
  code: image.edit
  inputs:
    image:
      type: asset.image
      required: true
    prompt:
      type: string
      required: true
    lora:
      type: model[]
      required: false
  outputs:
    image:
      type: asset.image
